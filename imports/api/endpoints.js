// this is the BO for API. Change this
export const Endpoint = 'http://localhost:8091';

// this is the endpoint for images, if provided the image came from the other source
export const publicEndpoint = 'http://localhost:8091';
